# Validação CI do pacote EasyPanel

O workflow `.github/workflows/easypanel-validation.yml` valida os arquivos estáticos e constrói as imagens de API, worker, frontend, Prometheus, Grafana e Alertmanager.

A homologação real ainda exige DNS, credenciais e acesso ao servidor.
